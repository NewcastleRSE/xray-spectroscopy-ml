$schema: https://json-schema.org/draft/2020-12/schema
$id: gemnet.schema.yaml
title: 'XANESNET dataset schema: gemnet'
$defs:
  datasetGemNet:
    type: object
    additionalProperties: false
    required:
    - dataset_type
    - root
    - graph_builder
    properties:
      graph_builder:
        $ref: ../graph_types.schema.yaml#/$defs/graphBuilder
        description: Main graph builder configuration.
      quadruplets:
        type: boolean
        default: false
        description: Whether to compute quadruplet indices.
      int_graph_builder:
        $ref: ../graph_types.schema.yaml#/$defs/nullableGraphBuilder
        default: null
        description: Interaction graph builder configuration. ``null`` reuses ``graph_builder``.
      oc_mode:
        type: boolean
        default: false
        description: Whether to precompute GemNet-OC auxiliary graphs and mixed triplets.
      oc_aeaint_graph_builder:
        $ref: ../graph_types.schema.yaml#/$defs/nullableGraphBuilder
        default: null
        description: Atom-edge-atom graph builder configuration. ``null`` reuses ``graph_builder``.
      oc_aint_graph_builder:
        $ref: ../graph_types.schema.yaml#/$defs/nullableGraphBuilder
        default: null
        description: Atom-atom graph builder configuration. ``null`` reuses ``graph_builder``.
      root:
        type: string
        description: Directory that stores processed ``.pth`` files.
      preload:
        type: boolean
        default: false
        description: Whether to preload processed samples.
      skip_prepare:
        type: boolean
        default: false
        description: Whether to reuse existing processed files.
      split_ratios:
        type:
        - array
        - 'null'
        items:
          type: number
          minimum: 0
        minItems: 1
        default:
        - 1.0
        $comment: Runtime requires the values to sum to 1.0 when split_indexfile is null.
        description: Optional split ratios.
      split_indexfile:
        type:
        - string
        - 'null'
        default: null
        description: Optional path to split indices.
      dataset_type:
        const: gemnet
        description: Registered dataset type name.
    description: Dataset for GemNet and GemNet-OC graph inputs.
description: Dataset schemas backed by ``DatasetRegistry`` classes and dataset constructor signatures.
