$schema: https://json-schema.org/draft/2020-12/schema
$id: e3ee_full.schema.yaml
title: 'XANESNET dataset schema: e3ee_full'
$defs:
  datasetE3EEFull:
    type: object
    additionalProperties: false
    required:
    - dataset_type
    - root
    - graph_builder
    - att_graph_builder
    properties:
      graph_builder:
        $ref: ../graph_types.schema.yaml#/$defs/graphBuilder
        description: Main graph builder configuration.
      att_graph_builder:
        $ref: ../graph_types.schema.yaml#/$defs/graphBuilder
        description: Attention graph builder configuration.
      use_path_branch:
        type: boolean
        default: false
        description: Whether to precompute site-centered paths.
      max_paths_per_site:
        type: integer
        minimum: 1
        default: 128
        description: Maximum paths saved per site.
      use_target_site_mask:
        type: boolean
        default: false
        description: Whether attention/path data are limited to target sites.
      root:
        type: string
        description: Directory that stores processed ``.pth`` files.
      preload:
        type: boolean
        default: true
        description: Whether to preload processed samples.
      skip_prepare:
        type: boolean
        default: false
        description: Whether to reuse existing processed files.
      split_ratios:
        type:
        - array
        - 'null'
        items:
          type: number
          minimum: 0
        minItems: 1
        default:
        - 1.0
        $comment: Runtime requires the values to sum to 1.0 when split_indexfile is null.
        description: Optional split ratios.
      split_indexfile:
        type:
        - string
        - 'null'
        default: null
        description: Optional path to split indices.
      dataset_type:
        const: e3ee_full
        description: Registered dataset type name.
    description: Full-structure E3EE dataset that emits one graph per structure.
description: Dataset schemas backed by ``DatasetRegistry`` classes and dataset constructor signatures.
